import express from "express";
import cors from "cors";

const app = express();
const port = 3000;
app.use(cors());

app.get

app.listen(port,()=>{
    console.log(`The server is ruuning on port ${port}`)
})